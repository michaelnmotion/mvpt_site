/**
 * ============================================
 *  PROMO BAR TOGGLE
 * ============================================
 *  To turn the promo ON:   set ENABLED to true
 *  To turn the promo OFF:  set ENABLED to false
 * ============================================
 */

const ENABLED = true;

module.exports = {
  enabled: ENABLED,
  showOnAllPages: false
};
